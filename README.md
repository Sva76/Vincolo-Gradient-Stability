# Vincolo-Gradient-Stability
A gradient stability controller for neural network training. It suppresses divergence under extreme learning rates, mitigates shock events, and enables fast recovery from catastrophic or noisy batches. Compatible with LoRA, full fine-tuning, and adapter-based methods.
